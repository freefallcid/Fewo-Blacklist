# Ubuntu 14.04 Trusty support
sudo: required
dist: trusty

language: cpp

addons:
  apt:
    sources:
    # add PPAs with more up-to-date toolchains
    - ubuntu-toolchain-r-test
    packages:
    # install toolchains
    - g++-4.8
    #- clang-3.6

compiler:
  #- clang
  - gcc

branches:
  only:
    - master

script:
  - make
  - make clean

before_deploy:

deploy:

after_deploy:

